<sl-checkbox 
  class="form__checkbox <%= env.classes %>"
  name="<%= env.name %>" 
  label="<%= env.label %>" 
  id="<%= env.id %>"
  data-validate="<%= env.validate %>"
>
  Check me before submitting
</sl-checkbox>